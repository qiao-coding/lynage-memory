# Lynage Memory 架构

## 1. 定位

**Lynage Memory 是面向长期运行 Agent 的上下文谱系记忆模块。**

它不是完整 Agent 框架，不负责模型调用、工具编排或工作流，而是嵌入现有 Agent，接管：

```text
消息保存
→ 最近上下文维护
→ 历史归档
→ 目录索引
→ 历史检索
→ 原文恢复
→ Prompt 上下文编译
```

核心定义：

> **无损历史存储 + 代际目录索引 + 共享上下文并行检索 + 按需原文重建。**

---

## 2. 核心目标

传统 Agent 通常在上下文达到上限后，用摘要替换旧对话。

Lynage 改为：

```text
完整对话
→ 原文不可变保存
→ 较旧消息归档为 Context Chunk
→ 生成只负责导航的目录描述
→ 目录达到容量后形成更高代目录
→ 需要时重新读取原文
```

它避免的是：

* 原文被摘要永久替代；
* 多次摘要导致语义漂移；
* 记忆脱离产生背景；
* 检索到孤立信息后重新猜测其作用。

但仍会对每轮实际输入进行选择和编译，因为单次模型窗口依然有限。

---

## 3. 核心原则

### 原文是唯一事实来源

```text
Original Messages = Source of Truth
Directory Summary = Navigation
Worker Result = Candidate Evidence
```

目录摘要和 Worker 判断都不能直接成为最终事实。

### 记忆必须保留谱系

每条重要信息需要能够恢复：

```text
从哪里产生
为什么产生
属于哪个阶段
修改了什么旧结论
后续如何变化
当前是否仍有效
原始消息在哪里
```

### 按时间和容量组织

不依赖开发者提前建立永久正确的语义分类。

目录主要根据：

```text
时间顺序
+ 节点容量
+ 归档代数
```

自动生长。

### 搜索允许走错

高层摘要可能不准确，但系统必须支持：

* 返回上层目录；
* 检查相邻目录；
* 使用全文索引；
* 持续遍历未检查范围；
* 最终读取原文验证。

---

## 4. 整体架构

```text
Agent Framework
├── Model
├── Tools
├── Workflow
└── Lynage Adapter
        │
        ▼
Lynage Memory Core
├── Source Store
├── Recent Context Manager
├── Working Memory
├── Archive Manager
├── Directory Indexer
├── Generation Compactor
├── History Retriever
├── Parallel Search Coordinator
├── Source Verifier
├── Context Compiler
└── Memory Action Validator
        │
        ▼
Storage
├── Messages
├── Context Chunks
├── Directories
├── Search Tasks
└── Working Memory
```

---

## 5. 核心数据层

### Source Store

不可变保存：

* User Message
* Assistant Message
* Tool Call
* Tool Result
* 文件操作
* Agent 执行结果

旧记录不原地修改。纠正信息时增加：

```text
supersedes
corrects
invalidates
```

关系。

### Recent Context

保存当前完整工作窗口：

```text
最近原始消息
+ 当前未完成任务
+ 工具执行状态
+ 少量边界重叠消息
```

它是正常对话默认使用的上下文。

### Working Memory

只保存当前继续工作需要的状态：

```json
{
  "当前目标": "完成 Lynage Memory 架构",
  "已经确定": "采用代际目录和原文验证",
  "当前进度": "设计并行检索流程",
  "未解决问题": "并发预算和目录容量"
}
```

它不是完整历史档案。

### User Memory

保存跨任务稳定信息，例如用户偏好、长期背景和固定约束。

---

## 6. Context Chunk

Recent Context 达到阈值后，较旧消息被归档为 Context Chunk。

```json
{
  "contextId": "ctx-028",
  "timeRange": "2026-07-25 12:00-13:30",
  "description": "讨论 Lynage 的共享上下文并行检索。",
  "progress": "确定多个 Worker 使用同一项目快照。",
  "changes": [
    "从串行搜索扩展为并行子目录读取"
  ],
  "sourceRange": {
    "from": "msg-820",
    "to": "msg-910"
  }
}
```

切分必须避开：

* 一轮问答中间；
* Tool Call 与 Tool Result 中间；
* 未完成决策过程；
* 正在执行的任务。

可保留少量重叠消息，保证边界连续。

---

## 7. 代际目录

目录达到容量上限后，不删除原节点，而是将它们降级为新父目录的子节点。

```text
Root
├── Recent
├── G0 Context Chunks
└── G1 Directory
    ├── G0 Directory A
    ├── G0 Directory B
    └── G0 Directory C
```

继续增长：

```text
Context Chunks
→ G1 Directory
→ G2 Directory
→ G3 Directory
```

Root 只保存当前直属节点，不包含全部历史。

每个目录需要描述：

* 时间范围；
* 这一阶段处理了什么；
* 项目如何推进；
* 形成了哪些结论；
* 哪些旧结论被修改；
* 尚未解决什么；
* 子节点分别保存什么；
* 原文如何下钻。

目录压缩的是**导航视图**，不是原始信息。

---

## 8. 普通检索流程

明确历史问题：

```text
用户问题
→ 读取 Root
→ 选择相关目录
→ 逐层下钻
→ 定位 Context Chunk
→ 打开 Source Range
→ 读取原文及相邻消息
→ 回答
```

可以同时使用确定性索引加速：

```text
时间范围
+ userId / threadId
+ messageId
+ SQLite FTS
+ 目录父子关系
```

正常明确查询的理想复杂度接近：

```text
O(log_B N + K)
```

其中：

* `B`：每个目录的最大子节点数；
* `N`：历史节点数量；
* `K`：最终候选数量。

---

## 9. 模糊搜索

极度模糊的问题创建持久化搜索任务：

```text
Search Task
├── 用户原始问题
├── 当前理解
├── 搜索目标
├── 已检查节点
├── 排除原因
├── 当前候选
├── 搜索游标
└── 下一批范围
```

搜索可以分批执行：

```text
读取三个子目录
→ 保存结果
→ 释放本批原文
→ 继续下一批
```

全部搜索后仍未命中，则返回：

```text
未找到精确记录
+ 已搜索范围
+ 最接近的信息
+ 可能的关联
+ 用户可能记错或原文缺失的说明
```

推测必须标注为推测。

---

## 10. Shared-Context Parallel Memory

这是 Lynage 的核心扩展能力。

主进程创建一个统一的项目快照：

```json
{
  "snapshotId": "state-v108",
  "projectGoal": "设计 Lynage Memory",
  "currentProgress": "确定并行历史检索",
  "knownDecisions": [
    "原文不可变",
    "目录摘要只负责导航"
  ],
  "question": "之前为什么放弃那个方案？",
  "searchGoal": "寻找方案提出、废弃和替代过程"
}
```

随后分配不同子目录：

```text
共享项目快照
├── Worker A：读取目录 A
├── Worker B：读取目录 B
├── Worker C：读取目录 C
└── Worker D：读取目录 D
```

每个 Worker：

* 对项目拥有相同理解；
* 使用同一个问题和判断标准；
* 只读取不同历史分片；
* 不修改 Working Memory；
* 不生成最终答案；
* 只返回证据位置。

```json
{
  "checkedNodes": ["dir-21", "dir-22"],
  "candidates": [
    {
      "sourceRange": {
        "from": "msg-820",
        "to": "msg-847"
      },
      "reason": "此处明确说明方案废弃原因",
      "confidence": 0.91
    }
  ]
}
```

---

## 11. 主进程验证与关联重建

主进程等待 Worker 完成后：

```text
合并候选
→ 去重和排序
→ 读取候选原文
→ 阅读前后消息
→ 搜索相关概念的其他位置
→ 重建信息演变链
→ 输出最终结论
```

最终恢复：

```text
方案首次提出
→ 讨论和尝试
→ 暴露问题
→ 正式废弃
→ 新方案替代
→ 当前有效状态
```

主进程不会重新读取 Worker 检查过的所有材料，只读取：

* 高价值候选原文；
* 必要的相邻消息；
* 相关历史位置。

---

## 12. 无界 Agent Context

Lynage 不会改变单个模型的上下文窗口。

它实现的是：

```text
有限模型窗口
+ 可扩展外部历史
+ 代际目录
+ 并行历史读取
+ 持久化搜索状态
+ 原文按需恢复
= Unbounded Agent Context
```

更严谨的定义是：

> Lynage 可以处理任意大的有限历史，容量受存储、计算、时间和成本限制，而不是受单次模型窗口限制。

极端全量扫描时：

```text
单进程：O(N)
P 个并行 Worker：理想情况下接近 O(N / P)
```

实际速度还会受到数据库吞吐、模型延迟和候选合并成本影响。

---

## 13. Context Compiler

底层结构不会完整发送给模型。

```text
Storage
→ Context Selection
→ Context Compiler
→ 精简 Markdown / Messages
→ Agent Model
```

正常对话只加载：

```text
User Memory
+ Working Memory
+ Recent Context
```

历史任务额外加载：

```text
相关目录描述
+ 已验证原文
+ 来源位置
```

不同场景生成不同视图：

```text
normal-chat
project-work
history-search
worker-search
source-verification
archive-indexing
```

---

## 14. 模型写回

模型只能提出增量记忆操作：

```json
{
  "reply": "已确定共享上下文并行检索方案。",
  "memoryActions": [
    {
      "target": "workingMemory",
      "operation": "append",
      "section": "已经确定",
      "value": "Worker 共享同一个项目上下文快照。"
    }
  ]
}
```

执行流程：

```text
JSON 解析
→ Schema 校验
→ 语义和权限校验
→ 数据库事务写入
```

模型不能：

* 覆盖整个 Memory；
* 修改原始消息；
* 删除完整历史目录；
* 将推测写为确定事实；
* 破坏 Source Range。

---

## 15. 存储设计

推荐第一版：

```text
SQLite
+ WAL
+ FTS5
```

核心表：

```text
messages
context_chunks
directories
directory_edges
working_memory
user_memory
search_tasks
search_progress
```

原则：

```text
SQLite = 唯一事实存储
JSON / Markdown = 动态生成的模型视图
```

达到超大规模后可以替换为：

```text
PostgreSQL
分片数据库
全文搜索服务
对象存储
分布式任务队列
```

Core 不绑定具体存储。

---

## 16. 对外接口

核心接口：

```ts
interface LynageMemory {
  startTurn(input: StartTurnInput): Promise<Turn>;
  appendMessage(input: MessageInput): Promise<void>;
  compile(input: CompileInput): Promise<CompiledContext>;
  search(input: SearchInput): Promise<SearchResult>;
  continueSearch(id: string): Promise<SearchResult>;
  openSource(input: SourceRange): Promise<Message[]>;
  commit(actions: MemoryAction[]): Promise<void>;
}
```

框架适配：

```text
lynage-memory
@lynage/vercel
@lynage/mastra
@lynage/langgraph
@lynage/mcp
```

---

## 17. 产品边界

Lynage 不开发：

```text
Agent Loop
模型 Provider
Tool Calling
Workflow
聊天 UI
认证系统
向量数据库
知识图谱
云部署平台
```

Lynage 专注开发：

```text
安全上下文切分
不可变原文存储
Context Chunk
代际目录压缩
上下文谱系
持久化搜索
共享上下文并行检索
原文验证
Context Compiler
Memory Action 校验
```

---

## 18. MVP

### 第一阶段

```text
完整消息保存
+ Recent Context
+ 单层 Context Chunk
+ 单层目录
+ 原文恢复
+ Context Compiler
```

### 第二阶段

```text
Working Memory
+ 代际目录压缩
+ FTS 辅助搜索
+ 搜索进度持久化
```

### 第三阶段

```text
共享上下文快照
+ 并行 Worker
+ 候选合并
+ Source Verifier
+ 信息演变链重建
```

### 第四阶段

```text
多框架 Adapter
+ 分布式存储
+ 并行任务调度
+ Benchmark 与 Devtools
```

---

## 19. 最终定义

### 名称

**Lynage Memory**

### 技术定位

**Context-lineage memory layer for AI agents.**

### 核心能力

```text
无损原文
+ 代际目录
+ 项目进度关联
+ 持久化历史搜索
+ 共享上下文并行阅读
+ 主进程原文验证
+ 按需上下文重建
```

### 核心价值

> Lynage 不要求模型一直携带全部历史，也不让摘要成为唯一记忆。它让有限上下文模型通过可扩展存储、目录索引和并行读取，持续访问并验证任意规模的历史信息。

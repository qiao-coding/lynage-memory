# Lynage Memory 架构

## 1. 定位

**Lynage Memory 是基于 Context Lineage 的 Agent 记忆与上下文管理模块。**

它负责：

* 保存完整原始对话
* 维护最近上下文
* 记录当前任务与项目进度
* 按时间和容量生成代际目录
* 将记忆与原始上下文保持关联
* 检索并恢复历史原文
* 为 Agent 编译当前所需上下文

它不负责：

* 模型调用
* Agent 调度
* Workflow 编排
* Tool 执行
* Skill 系统
* 多 Agent 协作

生态位置：

```text
Agent Framework
├── Model
├── Tools
├── Skills
├── Workflow
└── Lynage Memory
    ├── Recent Context
    ├── Working Memory
    ├── Context Archive
    ├── Generational Directory
    ├── History Search
    └── Context Compiler
```

一句话定位：

> Lynage Memory 是让 Agent 记忆始终保留原始上下文、时间位置和任务进度的长期记忆模块。

---

## 2. 核心问题

传统 Agent 通常采用：

```text
完整对话
→ 上下文达到上限
→ 生成压缩摘要
→ 摘要继续进入上下文
→ 旧原文依靠关键词重新搜索
```

问题在于：

* 摘要可能遗漏重要关系
* 多次压缩会造成语义漂移
* 记忆容易变成孤立事实
* Agent 不知道结论为何产生
* Agent 不知道信息属于哪个项目阶段
* 模糊问题需要猜关键词
* 找到旧消息后仍需重新推测其作用

Lynage Memory 改为：

```text
完整对话
→ 旧消息原样归档
→ 对归档段生成一次导航摘要
→ 目录达到容量后压缩目录
→ 原目录降级为子目录
→ 需要时逐层进入并读取原文
```

压缩的是**目录和索引视图**，不是原始信息。

---

## 3. 核心原则

### 3.1 原文是事实来源

用户消息、AI 回复、工具输入输出均以追加方式保存。

```text
原文 = Source of Truth
摘要 = Navigation
```

摘要不能替代原文承担最终事实判断。

### 3.2 最近对话保持完整

当前窗口保留最近的完整消息。

达到 Token 阈值后，仅将较旧部分归档，最近部分继续原样保留。

### 3.3 目录按时间与容量生成

不要求 AI 提前判断内容属于哪个项目或主题。

目录自然按照：

```text
时间顺序
+ 节点容量
+ 归档代数
```

逐步形成树状结构。

### 3.4 记忆必须保留上下文关系

Lynage 保存的不只是：

```text
“用户选择 SQLite”
```

还要能够恢复：

```text
为什么选择 SQLite
替代了什么方案
当时讨论到哪个阶段
后续如何使用这个决定
原始消息在哪里
```

### 3.5 搜索可验证、可继续

搜索结果必须能回到原文验证。

极度模糊的查询允许分批搜索并保存进度，而不是要求一次检索完成。

---

## 4. 整体数据结构

```text
.lynage/
├── user.json
├── memory.json
├── root-contents.json
├── recent/
│   └── active.jsonl
├── generations/
│   ├── g0/
│   ├── g1/
│   └── g2/
├── sources/
│   └── messages.jsonl
└── searches/
    └── search-{id}/
        ├── task.md
        └── search.md
```

---

## 5. 核心文件

### 5.1 `user.json`

保存跨任务相对稳定的用户信息。

```json
{
  "用户概况": "用户正在开发多个 AI 与前端相关项目。",
  "长期偏好": "回答需要简洁、直接，技术判断必须严谨。",
  "稳定约束": "不喜欢复杂流程图式 Agent 编辑器。"
}
```

仅保存较稳定的信息，例如：

* 表达偏好
* 长期目标
* 稳定背景
* 长期约束
* 反复确认的习惯

不应把当前任务临时状态写入 `user.json`。

---

### 5.2 `memory.json`

保存当前工作记忆。

```json
{
  "当前任务": "设计 Lynage Memory 架构。",
  "已经确定": "采用原文无损保存和代际目录压缩。",
  "当前进度": "正在确定模块边界和接入方式。",
  "未解决问题": "归档阈值和目录容量尚未确定。",
  "最近变化": "产品定位从完整 Agent 修正为独立 Memory 模块。"
}
```

主要内容：

* 当前目标
* 已确定结论
* 当前进度
* 未解决问题
* 最近变更
* 下一步可能动作

它不是历史档案，而是 Agent 当前继续工作的状态。

---

### 5.3 `root.json`

根目录只描述当前可见的直属节点。

```json
{
  "当前状态": "Lynage Memory 架构设计阶段。",
  "最近上下文": "./recent/active.jsonl",
  "目录": [
    {
      "时间范围": "2026-07-23 至 2026-07-24",
      "内容": "Agent 长期记忆、Hermes 对比和代际目录设计。",
      "进度": "已经确定 Lynage Memory 的核心定位。",
      "路径": "./generations/g1/dir-004/directory.json"
    }
  ]
}
```

Root 不保存全部历史目录，只保存：

* 最近活动上下文
* 当前工作状态
* 直属历史目录
* 向下读取路径

---

### 5.4 `directory.json`

每个归档目录的导航说明。

```json
{
  "时间范围": "2026-07-23 至 2026-07-24",
  "整体内容": "讨论 Agent 长期记忆和上下文恢复机制。",
  "推进过程": "从普通摘要记忆逐渐形成代际上下文目录方案。",
  "主要结论": [
    "原始消息永久保存",
    "目录摘要只用于导航",
    "目录按容量逐代压缩",
    "极度模糊搜索保存检索状态"
  ],
  "重要变化": [
    "放弃语义项目分类",
    "将产品定位修正为 Agent Memory 模块"
  ],
  "子节点": [
    {
      "内容": "Hermes 上下文压缩问题。",
      "作用": "形成 Lynage 的问题定义。",
      "路径": "./context-001/context.json"
    },
    {
      "内容": "代际目录和复杂度分析。",
      "作用": "确定历史信息的组织与检索方式。",
      "路径": "./context-002/context.json"
    }
  ]
}
```

---

## 6. 原始消息存储

所有消息使用不可变追加记录。

```json
{
  "messageId": "msg-1042",
  "sessionId": "session-28",
  "role": "user",
  "content": "那就 lynage-memory，整理架构",
  "createdAt": "2026-07-24T19:00:00-07:00"
}
```

建议保存：

* 用户消息
* AI 回复
* Tool Call
* Tool Result
* 文件修改记录
* 任务执行结果

原始记录不能被摘要覆盖或原地修改。

需要纠正时新增：

```text
supersedes
corrects
invalidates
```

关系，而不是删除旧记录。

---

## 7. 最近上下文机制

```text
Recent Context
├── 最近完整消息
├── 当前未完成任务
├── 当前工具调用状态
└── 少量边界重叠消息
```

归档触发条件可以基于：

* Token 数量
* 消息数量
* Prompt 预算占用
* 模型上下文窗口比例

归档时不能在以下位置切断：

* 用户问题和 AI 回答之间
* Tool Call 和 Tool Result 之间
* 未完成任务中间
* 一个完整决策过程内部

可以保留 2～5 轮重叠消息，增强边界连续性。

---

## 8. 上下文归档

流程：

```text
Recent Context 达到阈值
→ 找到最近自然边界
→ 将旧消息复制到 Context Chunk
→ 原始消息继续保存在 Source Store
→ AI 根据该段完整原文生成目录描述
→ 将 Chunk 加入当前 G0 目录
→ Recent Context 保留最新消息
```

Context Chunk 结构：

```json
{
  "contextId": "ctx-028",
  "timeRange": "2026-07-24 18:20-19:10",
  "summary": "讨论 Lynage Memory 的定位和命名。",
  "progress": "将 Lynage 从完整 Agent 修正为 Memory 模块。",
  "keywords": [
    "Lynage Memory",
    "Context Lineage",
    "Hermes",
    "Agent Memory"
  ],
  "sourceRange": {
    "from": "msg-980",
    "to": "msg-1042"
  }
}
```

摘要只生成一次，并保留具体原文范围。

---

## 9. 代际目录压缩

假设一个目录最多容纳 `B` 个节点。

### G0

保存最近归档的 Context Chunks：

```text
G0
├── ctx-001
├── ctx-002
└── ctx-020
```

达到容量后：

```text
20 个 Context Chunks
→ 创建 G1 Directory
→ 原 20 个节点降级为其子节点
```

### G1

```text
G1
├── dir-001
├── dir-002
└── dir-020
```

G1 达到容量后：

```text
20 个 G1 Directory
→ 创建 G2 Directory
→ 原目录整体降级
```

最终形成：

```text
Root
→ Generation Directory
→ Subdirectory
→ Context Chunk
→ Source Messages
```

越旧的信息通常层级越深，但原文始终存在。

---

## 10. 目录摘要内容

每次目录压缩时，AI应生成：

* 时间范围
* 这段时期主要处理了什么
* 任务如何推进
* 形成了哪些决定
* 哪些旧决定被修改
* 尚未完成的问题
* 各子节点的内容和作用
* 指向原始节点的路径

不能只生成普通主题摘要，例如：

```text
“讨论了 Agent 和 Memory。”
```

需要生成进度型描述：

```text
“这一阶段从比较 Hermes 的上下文压缩问题开始，
随后放弃语义项目分类，确定使用时间和容量驱动的
代际目录，并将 Lynage 定位为独立 Agent Memory 模块。”
```

---

## 11. 检索流程

### 11.1 默认不检索

普通连续对话只编译：

```text
user.json
+ memory.json
+ recent context
```

### 11.2 明确历史查询

```text
用户问题
→ 判断需要历史信息
→ 读取 root.json
→ 选择最相关直属目录
→ 逐层读取 directory.json
→ 定位 Context Chunk
→ 打开原始消息范围
→ 根据原文回答
```

### 11.3 混合检索

目录导航与传统索引可以并存：

```text
目录下钻
+ SQLite FTS
+ 时间范围过滤
+ 精确实体匹配
```

关键词搜索用于加速，不承担唯一检索入口。

### 11.4 极度模糊查询

例如：

```text
“之前那个设计为什么不用了？”
```

创建搜索任务：

```text
search-{id}/
├── task.md
└── search.md
```

`task.md`：

```md
# 用户问题

之前那个设计为什么不用了？

# 当前理解

用户没有说明项目、时间或具体设计。

# 搜索策略

从最近目录开始，每批检查三个子节点。
优先寻找被替换、放弃或修正的方案。
```

`search.md`：

```md
# 已检查

- dir-028：讨论产品命名，无关
- dir-027：出现定位修正，可能相关
- dir-026：放弃语义分类，可能相关

# 当前候选

1. Lynage 从完整 Agent 修正为 Memory 模块
2. 放弃语义项目分类

# 下一步

读取两个候选的原始消息并比较。
```

每批搜索后更新进度，避免重复检查。

---

## 12. 未找到时的处理

全量搜索后仍无精确结果：

```text
返回未命中状态
+ 已搜索范围
+ 最接近的信息
+ 接近信息之间的关系
+ 可能原因
```

示例：

```json
{
  "status": "not_found",
  "searched": "最近 4 个代际目录，共 82 个上下文块",
  "closest": [
    "曾放弃语义项目分类",
    "曾将产品从 Agent Runtime 修正为 Memory 模块"
  ],
  "inference": "用户可能记混了两个方案，或原记录已被删除。",
  "confidence": "low"
}
```

推测必须显式标记，不能伪装成历史事实。

---

## 13. Context Compiler

底层存储使用 JSON、JSONL、SQLite，但输入模型前编译成简洁文本。

```text
Storage
→ Context Selection
→ Context Compiler
→ Markdown / Text Prompt View
→ Agent Model
```

示例：

```md
# 用户偏好

回答简洁、直接，技术设计需要严谨。

# 当前任务

整理 Lynage Memory 架构。

# 已经确定

- Lynage 是 Agent Memory 模块，不是完整 Agent。
- 原始对话不可变保存。
- 使用时间和容量驱动的代际目录压缩。
- 摘要用于导航，最终事实读取原文。

# 最近对话

用户要求根据修正后的定位重新整理架构。
```

不同场景生成不同视图：

```text
normal-chat
project-work
history-search
tool-execution
archive-summary
```

---

## 14. 模型写回协议

Agent 正常回复时可以返回：

```json
{
  "reply": "Lynage Memory 的核心架构已经整理完成。",
  "memoryActions": [
    {
      "target": "memory",
      "operation": "append",
      "section": "已经确定",
      "value": "Lynage 的正式包名为 lynage-memory。"
    }
  ]
}
```

程序执行：

```text
解析 JSON
→ Schema 校验
→ 权限校验
→ 语义校验
→ 原子更新
```

原则：

> 模型只能提出记忆修改，Lynage Memory 才能真正执行。

禁止模型：

* 替换整个 `memory.json`
* 修改原始消息
* 删除完整目录
* 覆盖来源关系
* 写入未经验证的长期用户事实

---

## 15. 核心模块

```text
lynage-memory
├── Source Store
├── Recent Context Manager
├── Working Memory Manager
├── Archive Manager
├── Directory Indexer
├── Generation Compactor
├── History Retriever
├── Search Task Manager
├── Context Compiler
├── Memory Action Validator
└── Storage Adapter
```

### Source Store

保存不可变原始消息。

### Recent Context Manager

维护当前完整上下文和 Token 水位。

### Working Memory Manager

管理 `user.json` 和 `memory.json`。

### Archive Manager

将旧对话归档为 Context Chunk。

### Directory Indexer

生成目录导航、进度描述和原文范围。

### Generation Compactor

执行目录代际压缩和节点降级。

### History Retriever

沿目录路径查找并读取原文。

### Search Task Manager

保存模糊搜索任务和游标。

### Context Compiler

编译当前 Agent 所需的最小上下文。

### Memory Action Validator

校验并执行模型提出的记忆修改。

---

## 16. 存储层

推荐第一版使用 SQLite：

```text
SQLite
├── messages
├── context_chunks
├── directories
├── directory_children
├── working_memory
├── user_memory
├── search_tasks
└── search_progress
```

同时输出可读视图：

```text
.lynage/
├── root.json
├── user.json
├── memory.json
├── generations/
└── searches/
```

建议关系：

* SQLite：事务、索引、查询
* JSONL：原始消息追加日志
* JSON：状态和目录视图
* Markdown：搜索任务与模型阅读视图

---

## 17. 对外 API

### 初始化

```ts
const memory = createLynageMemory({
  storage: sqliteStorage("./lynage.db"),
  archiveThreshold: 12_000,
  directoryCapacity: 20,
});
```

### 编译上下文

```ts
const context = await memory.compile({
  sessionId,
  agentId,
  input,
});
```

### 保存消息

```ts
await memory.append({
  sessionId,
  role: "user",
  content: input,
});
```

### 历史搜索

```ts
const result = await memory.search({
  query,
  sessionId,
});
```

### 打开来源

```ts
const source = await memory.openSource({
  contextId,
  messageRange,
});
```

### 写回记忆

```ts
await memory.commit({
  actions: result.memoryActions,
});
```

---

## 18. Agent 生态接入

### Vercel AI SDK

```text
用户输入
→ Lynage compile
→ generateText / streamText
→ 保存模型回复
→ commit memoryActions
```

### Mastra / LangGraph

作为 Memory Provider 或 Context Middleware 接入。

### MCP

提供：

```text
lynage_memory_read
lynage_memory_search
lynage_memory_continue_search
lynage_memory_open_source
lynage_memory_commit
```

### 自研 Agent

只需接入三个主要接口：

```text
compile()
append()
commit()
```

历史需求出现时再调用：

```text
search()
openSource()
```

---

## 19. 包结构

```text
lynage-memory
核心入口包

@lynage/core
目录、归档、检索和上下文编译

@lynage/storage-sqlite
SQLite 存储实现

@lynage/mcp
MCP Server

@lynage/vercel
Vercel AI SDK Adapter

@lynage/mastra
Mastra Adapter

@lynage/langgraph
LangGraph Adapter

@lynage/devtools
查看目录树、搜索过程和来源关系
```

---

## 20. MVP

第一版只实现：

1. 原始消息追加保存
2. 最近完整上下文窗口
3. `user.json`
4. `memory.json`
5. Context Chunk 归档
6. 单层 `directory.json`
7. 目录达到阈值后代际压缩
8. Root → Directory → Chunk → Source 下钻
9. SQLite FTS 辅助搜索
10. `task.md + search.md` 模糊搜索
11. Context Compiler
12. 增量 Memory Actions 校验

第一版不做：

* 向量数据库
* 自动项目语义分类
* 多用户云同步
* 多 Agent 共享记忆
* 分布式存储
* 复杂权限系统
* 完整 Agent Runtime

---

## 21. 最终定义

### 产品名

**Lynage Memory**

### 包名

```text
lynage-memory
```

### 技术概念

**Context Lineage Memory**

### 产品定位

> 面向长期运行 Agent 的上下文谱系记忆模块。

### 核心架构

```text
原文无损存储
+ 最近完整上下文
+ 当前工作记忆
+ 代际目录压缩
+ 可下钻历史检索
+ 持久化模糊搜索
+ Prompt 上下文编译
```

### 与传统 Memory 的区别

传统 Memory 主要保存：

```text
Agent 应该记住什么
```

Lynage Memory 还保留：

```text
这条记忆从哪里产生
为什么产生
属于哪个阶段
改变了什么
如何影响当前任务
需要时去哪里读取原文
```

核心价值：

> 记忆不会脱离上下文成为孤立事实，Agent 可以持续恢复项目进度、决策过程和原始信息。

# Lynage Memory 开发者安装与使用

## 1. 定位

Lynage Memory 是面向 Agent 开发者的长期记忆与上下文模块。

开发者不需要创建一个新的 Lynage Agent，而是把 Lynage 安装进现有 Agent 项目：

```text
现有 Agent
├── Model
├── Tools
├── Workflow
└── Lynage Memory
```

Lynage 自动负责：

* 保存完整消息
* 维护最近上下文
* 记录当前任务状态
* 超过阈值后归档旧消息
* 生成代际目录索引
* 检索历史原文
* 编译模型需要的上下文
* 保存模型回复和工具结果

---

## 2. 推荐安装方式

### Vercel AI SDK

```bash
pnpm add lynage-memory @lynage/vercel ai
```

### Mastra

```bash
pnpm add lynage-memory @lynage/mastra
```

### 自研 Agent

```bash
pnpm add lynage-memory
```

### 跨语言或独立服务

```bash
pnpm add -g lynage-memory
lynage-memory serve
```

---

## 3. 基础初始化

```ts
import { createLynageMemory } from "lynage-memory";

export const memory = createLynageMemory({
  storage: {
    type: "sqlite",
    path: "./data/lynage.db",
  },

  archive: {
    tokenThreshold: 12_000,
    directoryCapacity: 20,
  },
});
```

默认使用 SQLite，本地项目不需要额外安装数据库。

第一次运行时自动创建：

```text
data/
└── lynage.db
```

---

## 4. Vercel AI SDK 使用

原来的调用：

```ts
const result = streamText({
  model,
  prompt: userInput,
  tools,
});
```

接入 Lynage 后：

```ts
import { lynageStreamText } from "@lynage/vercel";
import { memory } from "./memory";

const result = await lynageStreamText({
  memory,

  userId: "user-001",
  threadId: "thread-001",

  model,
  prompt: userInput,
  tools,
});
```

Lynage 内部自动执行：

```text
保存用户输入
→ 读取最近完整上下文
→ 加载当前工作记忆
→ 编译模型消息
→ 注册历史检索工具
→ 调用 streamText
→ 保存回复和工具结果
→ 检查归档阈值
→ 必要时生成 Context Chunk
```

开发者不需要手动调用：

```text
append()
compile()
archive()
commit()
```

---

## 5. Mastra 使用

```ts
import { Agent } from "@mastra/core";
import { LynageMemory } from "@lynage/mastra";

const agent = new Agent({
  name: "assistant",
  model,

  memory: new LynageMemory({
    storage: {
      type: "sqlite",
      path: "./data/lynage.db",
    },
  }),
});
```

之后继续使用 Mastra 原来的 Agent API：

```ts
const result = await agent.stream(userInput, {
  resourceId: "user-001",
  threadId: "thread-001",
});
```

映射关系：

```text
resourceId → Lynage userId
threadId   → Lynage threadId
```

Lynage Adapter 自动处理消息转换、上下文注入和回复保存。

---

## 6. 自研 Agent 使用

自研框架使用 Turn API。

```ts
const turn = await memory.startTurn({
  userId: "user-001",
  threadId: "thread-001",
  input: userInput,
});
```

`startTurn()` 返回：

```ts
{
  messages,
  tools,
  context,
  finish
}
```

将它交给自己的 Agent：

```ts
const result = await runAgent({
  messages: turn.messages,
  tools: {
    ...businessTools,
    ...turn.tools,
  },
});
```

本轮结束后：

```ts
await turn.finish({
  response: result.text,
  toolCalls: result.toolCalls,
  toolResults: result.toolResults,
});
```

完整流程：

```ts
const turn = await memory.startTurn({
  userId,
  threadId,
  input,
});

const result = await runAgent({
  messages: turn.messages,
  tools: {
    ...tools,
    ...turn.tools,
  },
});

await turn.finish(result);
```

---

## 7. 历史搜索工具

Lynage 默认可以向 Agent 提供：

```text
lynage_search
lynage_continue_search
lynage_open_source
```

Agent 遇到历史问题时可以调用：

```ts
const result = await memory.search({
  userId: "user-001",
  threadId: "thread-001",
  query: "之前为什么放弃语义分类？",
});
```

返回：

```ts
{
  status: "found",
  candidates: [
    {
      contextId: "ctx-028",
      summary: "讨论并放弃语义项目分类。",
      sourceRange: {
        from: "msg-820",
        to: "msg-847",
      },
    },
  ],
}
```

读取原文：

```ts
const messages = await memory.openSource({
  contextId: "ctx-028",
});
```

目录摘要只负责定位，最终事实以原始消息为准。

---

## 8. 极度模糊搜索

例如用户询问：

```text
之前那个方案为什么不用了？
```

Lynage 可以创建持久化搜索任务：

```ts
const search = await memory.startSearch({
  userId,
  query: "之前那个方案为什么不用了？",
});
```

继续搜索：

```ts
const batch = await search.next({
  limit: 3,
});
```

每批保存：

* 已检查目录
* 排除原因
* 当前候选
* 搜索游标
* 下一批范围

因此搜索可以连续执行，不会重复读取已经检查过的目录。

---

## 9. Working Memory

读取当前工作状态：

```ts
const state = await memory.getWorkingMemory({
  userId,
  threadId,
});
```

返回：

```ts
{
  currentTask: "设计 Lynage Memory",
  confirmed: [
    "原文不可变保存",
    "目录按时间和容量压缩",
  ],
  unresolved: [
    "目录容量阈值",
  ],
}
```

更新时使用增量操作：

```ts
await memory.commit([
  {
    target: "workingMemory",
    operation: "append",
    section: "confirmed",
    value: "正式包名为 lynage-memory",
  },
]);
```

不允许模型直接覆盖完整 Memory 文件。

---

## 10. 用户长期记忆

保存稳定用户信息：

```ts
await memory.commit([
  {
    target: "userMemory",
    operation: "append",
    section: "preferences",
    value: "用户偏好简洁、直接的回答。",
  },
]);
```

适合保存：

* 长期偏好
* 稳定背景
* 固定约束
* 长期目标

不适合保存：

* 临时问题
* 一次性任务
* 未确认推测
* 短期工具状态

---

## 11. 多用户与多会话

Lynage 使用两个主要标识：

```text
userId   → 用户级长期记忆
threadId → 当前会话或任务上下文
```

示例：

```ts
await lynageStreamText({
  memory,
  userId: "user-001",
  threadId: "lynage-project",
  model,
  prompt,
});
```

同一个用户可以拥有多个 Thread：

```text
user-001
├── lynage-project
├── zero-galgame
└── casual-chat
```

稳定用户信息可以共享，具体任务上下文相互隔离。

---

## 12. 跨语言使用

启动 Lynage Server：

```bash
lynage-memory serve \
  --database ./data/lynage.db \
  --port 4318
```

HTTP 接口：

```text
POST /v1/turns/start
POST /v1/turns/finish
POST /v1/memory/search
POST /v1/memory/open-source
GET  /v1/memory/state
```

Python 示例：

```python
import requests

turn = requests.post(
    "http://localhost:4318/v1/turns/start",
    json={
        "userId": "user-001",
        "threadId": "thread-001",
        "input": "继续之前的设计",
    },
).json()

messages = turn["messages"]
```

也可以通过 MCP 使用：

```bash
lynage-memory mcp
```

---

## 13. 开发环境调试

启动 Devtools：

```bash
lynage-memory dev
```

查看：

* 最近完整上下文
* 当前 Working Memory
* Context Chunks
* 代际目录结构
* 原始消息来源
* 搜索路径
* 未完成搜索任务
* Token 水位

Devtools 只用于调试，不参与实际存储。

---

## 14. 推荐默认配置

```ts
const memory = createLynageMemory({
  storage: {
    type: "sqlite",
    path: "./data/lynage.db",
  },

  recentContext: {
    maxTokens: 16_000,
    retainTokens: 6_000,
  },

  archive: {
    directoryCapacity: 20,
    overlapTurns: 3,
  },

  search: {
    batchSize: 3,
    enableFTS: true,
  },
});
```

默认行为：

```text
达到 16K Token
→ 选择自然边界
→ 归档旧消息
→ 保留最近约 6K Token
→ 边界重叠 3 轮
```

---

## 15. 包结构

```text
lynage-memory
默认核心包和 SQLite 实现

@lynage/vercel
Vercel AI SDK Adapter

@lynage/mastra
Mastra Adapter

@lynage/langgraph
LangGraph Adapter

@lynage/mcp
MCP Server

@lynage/devtools
目录和搜索调试工具
```

第一版建议只发布：

```text
lynage-memory
@lynage/vercel
```

---

## 16. 最小 Quick Start

```bash
pnpm add lynage-memory @lynage/vercel ai
```

```ts
import { createLynageMemory } from "lynage-memory";
import { lynageStreamText } from "@lynage/vercel";

const memory = createLynageMemory({
  storage: {
    type: "sqlite",
    path: "./data/lynage.db",
  },
});

export async function chat(input: string) {
  return lynageStreamText({
    memory,
    userId: "user-001",
    threadId: "default",
    model,
    prompt: input,
  });
}
```

核心体验：

> 安装 Lynage，包住原有模型调用，自动接管 Agent 的消息保存、上下文编译、历史归档和长期检索。

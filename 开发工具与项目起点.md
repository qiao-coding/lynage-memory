# Lynage Memory 开发工具与项目起点

## 1. 总体原则

Lynage Memory 是独立的 Agent Memory 模块，核心不能绑定：

* Mastra
* Vercel AI SDK
* LangGraph
* Hermes
* 特定模型厂商

正确结构：

```text
Agent Framework
      ↓ Adapter
Lynage Memory Core
      ↓
Storage / Model / Search
```

开发时需要一个最小 Agent 验证真实效果，但这个 Agent 只是：

* 测试宿主
* Benchmark 环境
* Quick Start 示例
* API 设计验证器

不是 Lynage 产品本身。

---

## 2. 项目真正的起点

第一项核心功能定为：

> **Lineage Conversation History：可归档、可追溯、可恢复的对话历史。**

先完成这个最小闭环：

```text
保存完整消息
→ 维护最近上下文
→ 超过 Token 阈值
→ 按自然边界归档旧消息
→ 生成单层目录索引
→ 从索引重新打开原始消息
→ 编译成下一轮 Agent 上下文
```

第一版只证明五件事：

1. 所有原始消息完整保存。
2. 当前 Prompt 不会持续增长。
3. 归档目录能正确定位历史。
4. Agent 能恢复旧决策产生的上下文。
5. 搜索结果可以追溯到原始消息。

暂时不做多代目录树。

---

## 3. 基础开发工具

### 运行环境

```text
Node.js
TypeScript strict mode
pnpm workspace
```

第一版使用简单 pnpm Monorepo，不需要立即引入 Turborepo。

### 构建工具

推荐：

```text
开发运行：tsx
类型检查：tsc --noEmit
包构建：tsdown
```

`tsdown`面向 TypeScript 库构建，支持声明文件、多种模块格式和依赖外置，适合后续发布 `lynage-memory` npm 包。

---

## 4. Reference Agent

推荐使用：

```text
Vercel AI SDK
```

只复用它的：

* 模型 Provider 统一
* `generateText`
* `streamText`
* Tool Calling
* 结构化输出
* 流式事件处理

AI SDK 已经提供文本生成、流式生成、工具调用和结构化输出能力，因此 Lynage 不需要重新开发 Agent Loop 或模型适配层。

目录：

```text
apps/
└── reference-agent/
    ├── agent.ts
    ├── tools.ts
    └── benchmark.ts
```

初始 Agent 只需要：

```text
用户输入
→ streamText
→ 一两个测试工具
→ 输出回复
```

然后再由 Lynage 包装它的每轮生命周期。

---

## 5. 数据库存储

推荐 MVP：

```text
SQLite
+ better-sqlite3
+ Drizzle ORM
+ Drizzle Kit
```

### SQLite 负责

* 原始消息
* Tool Call / Tool Result
* Context Chunk
* 目录节点
* 父子关系
* Working Memory
* 搜索进度

SQLite 自带 FTS5 全文搜索；WAL 模式适合同时存在读取和写入的本地 Agent 场景。

### Drizzle 负责

* TypeScript Schema
* 类型推断
* 数据库迁移
* 参数化查询

Drizzle 官方支持 `better-sqlite3`、`libSQL` 和 `node:sqlite` 等 SQLite 驱动，并提供迁移工具。

第一版建议使用 `better-sqlite3`。底层通过接口隔离：

```ts
interface LynageStore {
  appendMessage(input: MessageInput): Promise<void>;
  getRecent(scope: Scope): Promise<Message[]>;
  createChunk(input: ContextChunk): Promise<void>;
  createDirectory(input: DirectoryNode): Promise<void>;
  openSource(range: SourceRange): Promise<Message[]>;
}
```

以后再增加：

```text
@lynage/storage-libsql
@lynage/storage-postgres
```

---

## 6. 数据事实来源

只保留一套真实数据：

```text
SQLite = Source of Truth
```

不要同时维护：

```text
SQLite
+ JSON
+ JSONL
+ Markdown
```

四套需要同步的数据。

JSON 和 Markdown 只作为运行时生成的视图：

```text
SQLite
→ Context Compiler
→ Markdown / TXT
→ 模型
```

搜索任务中的 `TASK.md`、`SEARCH.md`，底层也应存入数据库，Markdown 只是模型读取格式。

---

## 7. Schema 与模型输出

使用：

```text
Zod
```

负责校验：

* Context Chunk Summary
* Directory Summary
* Memory Action
* Search State
* Source Range
* Adapter 输入输出

Zod 提供 TypeScript 类型推断和运行时数据校验，适合处理不可信的模型输出。

流程：

```text
AI 返回 JSON
→ Zod 格式校验
→ Lynage 语义校验
→ 数据库事务写入
```

Zod 只验证格式，Lynage 自己验证：

* 路径是否存在
* 是否越权修改
* 是否覆盖原始消息
* 是否重复归档
* Source Range 是否有效
* 是否删除关键记忆

---

## 8. 模型能力接口

Core 不直接依赖 AI SDK。

定义独立接口：

```ts
interface LynageModel {
  summarizeChunk(
    input: ChunkSummaryInput,
  ): Promise<ChunkSummary>;

  summarizeDirectory(
    input: DirectorySummaryInput,
  ): Promise<DirectorySummary>;

  analyzeSearchBatch(
    input: SearchBatchInput,
  ): Promise<SearchBatchResult>;
}
```

然后提供：

```text
@lynage/model-ai-sdk
```

这样以后可以接入：

* OpenAI SDK
* Anthropic SDK
* Ollama
* 本地模型
* Agent 框架自带模型层

---

## 9. 搜索能力

第一版直接复用：

```text
SQLite FTS5
+ 时间范围
+ messageId 范围
+ directoryId 路径
```

不要一开始使用：

* 向量数据库
* Embedding 模型
* 知识图谱
* 自动实体图
* 语义项目分类

FTS 只用于候选加速。

Lynage 的核心检索仍然是：

```text
Root
→ Directory
→ Context Chunk
→ Source Messages
```

向量搜索以后可以作为额外 Adapter，但不能成为唯一入口。

---

## 10. 测试工具

### 普通测试

```text
Vitest
```

用于：

* 单元测试
* 数据库集成测试
* Adapter 测试
* Context Compiler 快照测试

Vitest 支持 TypeScript、覆盖率和按依赖关系重新运行相关测试。

### 状态与不变量测试

```text
fast-check
```

用于生成大量随机消息、目录和归档操作，验证：

* 消息不会丢失
* 所有 Chunk 都能找到 Source
* 目录压缩后叶子节点数量不变
* 重复执行归档不会产生副本
* Root 可以到达全部消息
* 搜索游标不会重复检查节点

`fast-check` 是 JavaScript/TypeScript 的属性测试库，并支持与 Vitest 集成。

### 长期记忆 Benchmark

使用：

```text
LongMemEval
```

它包含长期信息提取、多会话推理、知识更新、时间推理和拒答测试，可用于比较 Lynage 与普通上下文压缩。

额外建立 Lynage 自己的项目连续性数据集：

```text
设计变更
方案废弃
工具执行
跨阶段引用
模糊历史问题
用户记错时拒绝确认
```

---

## 11. Hermes 复用边界

Hermes 不作为项目代码底座。

Hermes 是完整 Agent，包含 CLI、Gateway、工具、Skills、Session 和 Memory。Lynage 只需要研究它已经验证过的存储与插件设计。

可以参考：

* SQLite Session Storage
* WAL
* FTS5
* 完整消息保存
* Session Lineage
* Schema Migration
* Memory Provider 生命周期
* `prefetch`
* `sync_turn`
* `on_pre_compress`

Hermes 当前使用 SQLite 保存完整消息，并建立 FTS5 和 CJK trigram 索引；它的外部 Memory Provider 通过初始化、预取、同步和工具接口接入。

但不要复制：

* Hermes Agent Loop
* CLI
* Gateway
* Tools Runtime
* Skills
* Messaging
* 模型配置系统
* 原有压缩流程

Lynage 后期可以提供独立的 Hermes 插件，而不是 fork Hermes。

---

## 12. Mastra 的使用位置

Mastra 不作为 Core 依赖，而作为第一个 Agent Framework Adapter。

Mastra 已经具备：

* Thread / Resource
* 消息历史
* Working Memory
* Semantic Recall
* Observational Memory
* 可替换存储后端

因此 Lynage 不需要为 Mastra 用户重新发明 Thread 和 Agent 生命周期。

后期实现：

```text
@lynage/mastra
```

负责把：

```text
Mastra resourceId → Lynage userId
Mastra threadId   → Lynage threadId
Mastra messages   → Lynage messages
```

Mastra 的 Observational Memory 会把历史压缩为 observations 和更高层 reflections；它应当作为 Lynage 的对照组，而不是与 Lynage 同时启用。

---

## 13. 推荐仓库结构

```text
lynage-memory/
├── apps/
│   └── reference-agent/
│
├── packages/
│   ├── core/
│   ├── storage-sqlite/
│   └── adapter-ai-sdk/
│
├── benchmarks/
│   ├── baseline/
│   ├── longmemeval/
│   └── project-continuity/
│
├── drizzle/
├── pnpm-workspace.yaml
└── package.json
```

第一版只有三个包：

```text
@lynage/core
@lynage/storage-sqlite
@lynage/ai-sdk
```

暂时不创建：

```text
@lynage/mastra
@lynage/langgraph
@lynage/mcp
@lynage/devtools
```

---

## 14. 开发依赖

```bash
pnpm add ai zod drizzle-orm better-sqlite3

pnpm add -D \
  typescript \
  tsx \
  tsdown \
  drizzle-kit \
  vitest \
  fast-check \
  @types/better-sqlite3
```

---

## 15. 开发阶段

### M0：普通 Agent 基线

建立最小 AI SDK Agent：

```text
streamText
+ Tool Calling
+ SQLite 消息记录
```

不包含 Lynage。

目的是得到传统 Agent 的延迟、Token 和记忆准确率基线。

### M1：Turn 生命周期

实现：

```ts
startTurn()
finishTurn()
```

自动保存：

* User Message
* Assistant Message
* Tool Call
* Tool Result
* Token Usage

### M2：单层归档

实现：

```text
Recent 达到阈值
→ 查找自然边界
→ 创建 Context Chunk
→ 生成目录描述
→ Recent 保留最新部分
```

这是 Lynage 第一个真正有价值的版本。

### M3：原文恢复

实现：

```text
searchDirectory()
openSource()
compileRetrievedContext()
```

验证 Agent 是否能找到旧结论及其产生原因。

### M4：与普通压缩对比

比较：

* 历史问题准确率
* 项目继续能力
* 原文命中率
* Prompt Token
* 检索延迟
* 错误记忆率

没有证明单层方案有效前，不开发多层树。

### M5：代际目录

单层节点数量达到阈值后：

```text
G0 Nodes
→ G1 Directory
→ 原 G0 节点降级
```

### M6：模糊搜索状态

实现：

```text
Task
Search Progress
Checked Nodes
Candidates
Cursor
Next Batch
```

### M7：生态 Adapter

开发顺序：

```text
AI SDK
→ Mastra
→ LangGraph
→ MCP
→ Hermes
```

---

## 16. 明确不重复开发

Lynage 不开发：

```text
Agent Loop
模型 Provider
Tool Calling
流式响应协议
Agent Workflow
数据库引擎
ORM
全文搜索引擎
向量数据库
知识图谱
聊天 UI
认证系统
云部署平台
```

Lynage 只开发：

```text
安全上下文切分
原文与 Chunk 的来源关联
项目进度型目录摘要
目录代际压缩
可下钻历史恢复
持久化模糊搜索
Context Compiler
Memory Action 语义校验
```

---

## 17. 最终项目起点

### 开发宿主

> 基于 Vercel AI SDK 的最小 Reference Agent。

### 存储基础

> SQLite + FTS5 + WAL + Drizzle。

### 第一项 Lynage 功能

> 对话超过阈值后，将旧消息归档为可追溯 Context Chunk，并通过单层目录重新读取原文。

### 第一阶段成功标准

> 在长对话超过模型上下文后，Lynage 能比普通摘要压缩更准确地恢复项目决策、产生原因和原始消息，同时保持当前 Prompt 长度稳定。
